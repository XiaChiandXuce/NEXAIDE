{
  "name": "nexaide",
  "displayName": "NEXAIDE",
  "icon": "src/assets/nexaide-logo-128.png",
  "description": "NEXAIDE is an intelligent programming assistant extension, providing AI-based code completion, error fixing, and debugging features to enhance coding efficiency.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.101.2"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "nexaide.helloWorld",
        "title": "Hello World"
      },
      {
        "command": "nexaide.openChat",
        "title": "Open AI Chat",
        "icon": "$(comment-discussion)"
      },
      {
        "command": "nexaide.clearChat",
        "title": "Clear Chat History",
        "icon": "$(clear-all)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "nexaide-sidebar",
          "title": "NEXAIDE AI Assistant",
          "icon": "$(robot)"
        }
      ]
    },
    "views": {
      "nexaide-sidebar": [
        {
          "type": "webview",
          "id": "nexaide.chatView",
          "name": "AI Chat",
          "when": "true"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "nexaide.clearChat",
          "when": "view == nexaide.chatView",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "NEXAIDE",
      "properties": {
        "nexaide.mcp.connectTimeoutMs": {
          "type": "number",
          "default": 120000,
          "minimum": 1000,
          "markdownDescription": "MCP 连接超时时间（毫秒）。用于客户端连接 stdio MCP 服务器时的初始握手超时。"
        },
        "nexaide.agentBackend": {
          "type": "string",
          "enum": [
            "codex",
            "trae"
          ],
          "default": "codex",
          "markdownDescription": "默认使用的 Agent 后端。`codex` 依赖 Codex CLI，`trae` 使用本地 Trae-Agent。"
        },
        "nexaide.codex.binaryPath": {
          "type": "string",
          "default": "",
          "markdownDescription": "（可选）Codex CLI 可执行文件的完整路径，例如 `C:/Users/me/AppData/Local/Programs/codex/codex.exe`。留空则按 PATH 自动查找。"
        },
        "nexaide.traeAgent.path": {
          "type": "string",
          "default": "",
          "markdownDescription": "（可选）Trae-Agent 仓库路径（包含 `trae-cli` 的目录）。留空则在当前工作区及其父目录自动检测。"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.101.0",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.25.1",
    "http-server": "^14.1.1",
    "ts-loader": "^9.5.2",
    "typescript": "^5.8.3",
    "webpack": "^5.99.7",
    "webpack-cli": "^6.0.1"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.19.1"
  }
}
